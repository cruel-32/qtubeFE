import React from 'react';
import { SvgProps } from 'react-native-svg';
import SvgComponent from '@/assets/icons/facebook.svg';

const FacebookIcon = (props: SvgProps) => (
  <SvgComponent {...props} />
);

export default FacebookIcon;