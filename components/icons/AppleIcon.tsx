import React from 'react';
import { SvgProps } from 'react-native-svg';
import SvgComponent from '@/assets/icons/apple.svg';

const AppleIcon = (props: SvgProps) => (
  <SvgComponent {...props} />
);

export default AppleIcon;
