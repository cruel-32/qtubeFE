import React from 'react';
import { SvgProps } from 'react-native-svg';
import SvgComponent from '@/assets/icons/flag.svg';

const FlagIcon = (props: SvgProps) => (
  <SvgComponent {...props} />
);

export default FlagIcon;