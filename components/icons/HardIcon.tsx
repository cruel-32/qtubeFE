import React from 'react';
import { SvgProps } from 'react-native-svg';
import SvgComponent from '@/assets/icons/hard.svg';

const HardIcon = (props: SvgProps) => (
  <SvgComponent {...props} />
);

export default HardIcon;