import React from 'react';
import { SvgProps } from 'react-native-svg';
import SvgComponent from '@/assets/icons/info.svg';

const InfoIcon = (props: SvgProps) => (
  <SvgComponent {...props} />
);

export default InfoIcon;