import React from 'react';
import { SvgProps } from 'react-native-svg';
import SvgComponent from '@/assets/icons/media.svg';

const MediaIcon = (props: SvgProps) => (
  <SvgComponent {...props} />
);

export default MediaIcon;