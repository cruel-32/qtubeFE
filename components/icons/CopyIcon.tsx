import React from 'react';
import { SvgProps } from 'react-native-svg';
import SvgComponent from '@/assets/icons/copy.svg';

const CopyIcon = (props: SvgProps) => (
  <SvgComponent {...props} />
);

export default CopyIcon;