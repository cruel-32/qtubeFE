import React from 'react';
import { SvgProps } from 'react-native-svg';
import SvgComponent from '@/assets/icons/personal.svg';

const PersonalIcon = (props: SvgProps) => (
  <SvgComponent {...props} />
);

export default PersonalIcon;