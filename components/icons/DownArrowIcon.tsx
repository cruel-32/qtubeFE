import React from 'react';
import { SvgProps } from 'react-native-svg';
import SvgComponent from '@/assets/icons/down_arrow.svg';

const DownArrowIcon = (props: SvgProps) => (
  <SvgComponent {...props} />
);

export default DownArrowIcon;