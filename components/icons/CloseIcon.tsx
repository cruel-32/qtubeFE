import React from 'react';
import { SvgProps } from 'react-native-svg';
import SvgComponent from '@/assets/icons/close.svg';

const CloseIcon = (props: SvgProps) => (
  <SvgComponent {...props} />
);

export default CloseIcon;