import React from 'react';
import { SvgProps } from 'react-native-svg';
import SvgComponent from '@/assets/icons/beaker.svg';

const BeakerIcon = (props: SvgProps) => (
  <SvgComponent {...props} />
);

export default BeakerIcon;