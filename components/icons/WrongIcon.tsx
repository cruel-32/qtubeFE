import React from 'react';
import { SvgProps } from 'react-native-svg';
import SvgComponent from '@/assets/icons/wrong.svg';

const WrongIcon = (props: SvgProps) => (
  <SvgComponent {...props} />
);

export default WrongIcon;